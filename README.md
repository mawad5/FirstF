# FirstF

## How the Command works


## AI Assisted Programming Reflection

Step 1:

process.argv - checks command-line inputs to capture the filename and line count parameter

fs.existsSync - makes sure the target file exists on the filesystem before it opens it

fs.readFileSync - reads the contents of the target file into memory

process.argv.length - (for firstf.js) expects  3 user inputs (PATTERN, FILENAME, NUMBER_OF_LINES)


Step 2:

I asked AI for help with definitions, like what is grep, head, filefirst.

AI helped me with the terminal commands and checked my code to see what implementations I can make.

I think I was independent in terms of writing the actual code itself.

It initially confused me with command files for the terminal, but once I entered the right prompt, it worked.
